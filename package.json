{
  "name": "lawn-business-os",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android": "npx cap open android",
    "ios:add": "npx cap add ios",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
